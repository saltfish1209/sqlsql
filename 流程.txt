输入: "协议库存20230421这批的采购方式是啥？"
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 0: 预处理                                       │
│   to_halfwidth() → 全角转半角                         │
│   "协议库存20230421这批的采购方式是啥?"                  │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│ Step 1: A路预排序（无实体）                            │
│   SchemaLinker.hybrid_retrieve(question, [])          │
│     └─ CrossEncoder 对 60个「问题×列描述」对打分         │
│     └─ 返回排序列表，取 Top-6:                         │
│        [计划批次名称, 采购方式描述, 框架协议编号, ...]     │
│   build_entity_schema(Top-6) → 不含 Examples 的精简Schema│
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│ Step 2: 实体提取 (EntityExtractor)                    │
│   ┌─ LLM提取: 精简Schema + Few-Shot → LLM              │
│   │   → ["协议库存20230421"]                            │
│   ├─ 规则提取: 正则匹配中文+数字 / 纯数字               │
│   │   → ["协议库存20230421", "20230421"]                │
│   └─ 合并去重 → ["协议库存20230421", "20230421"]         │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│ Step 3: 带实体的完整混合召回                           │
│   SchemaLinker.hybrid_retrieve(question, entities)    │
│   ├─ B路: exact_index["协议库存20230421"]              │
│   │   → 命中列: {计划批次名称}                          │
│   ├─ A路: CrossEncoder精排 → Top-6排序列               │
│   ├─ C路: LSH(未B路命中的实体, A路Top-6列)              │
│   │   → 模糊匹配 + 二级校验                            │
│   └─ 返回:                                            │
│     ranked = [(列名,分数), ...]   ← 60列完整排序         │
│     must_have = {计划批次名称}    ← B/C路必须包含列       │
│     evidence = {exact_matches, fuzzy_matches}          │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│ Step 4: 梯队1尝试 (Top-6 + must_have 列)              │
│                                                       │
│  4a. 构建 M-Schema Prompt                             │
│      build_m_schema_prompt(tier1_cols, metadata)       │
│      + profile_text (列统计信息注入)                    │
│                                                       │
│  4b. 三路并发生成 (SQLGenerator)                       │
│      ├─ Thinking路: 深度推理 (temp=0.5) → 1条SQL        │
│      ├─ ICL路: Top-3相似模板 (temp=0.1) → 1条SQL        │
│      └─ Direct路: 直接生成 (temp=0.0) → 1条SQL          │
│      → 共3个候选SQL                                    │
│                                                       │
│  4c. 预检+修正 (SQLRefiner)                            │
│      对每个候选SQL:                                     │
│      ├─ 直接执行 → 成功？                               │
│      │   ├─ 是 → Literal校验 (WHERE值是否存在于列)       │
│      │   │   ├─ 通过 → status=success                   │
│      │   │   └─ 不通过 → 进入修复                       │
│      │   └─ 否 → 进入修复循环(最多2轮):                  │
│      │       └─ 错误反馈 + Schema + 合法列名 → LLM修正    │
│      └─ 返回 refined_candidates (带 status + result)     │
│                                                       │
│  4d. 投票选择 (SQLSelector)                            │
│      ├─ 按执行结果分组                                  │
│      ├─ 多数票胜出 → 返回best                           │
│      ├─ 平票 → resolve_tie (thinking > ICL > direct)    │
│      └─ 全部失败 → 返回 None                            │
└──────────────────────┬──────────────────────────────┘
           ┌───────────┴────────────┐
           │ best.status=="success"? │
           └───┬───────────────┬────┘
              是              否
               │               ▼
               │     ┌──────────────────────┐
               │     │ 梯队2: Top-12列+must  │
               │     │ (包含梯队1的列+新增列) │
               │     │ 重复 4a-4d            │
               │     └─────────┬────────────┘
               │          还是失败?
               │               ▼
               │     ┌──────────────────────┐
               │     │ 梯队3: Top-13~18列    │
               │     │ (全新列段兜底)         │
               │     │ 重复 4a-4d            │
               │     └─────────┬────────────┘
               ▼               ▼
┌─────────────────────────────────────────────────────┐
│ Step 5: 封装输出                                      │
│   {                                                   │
│     "final_sql": "SELECT 采购方式描述 FROM ...",        │
│     "execution_result": [("国网协议库存实际需求",)],     │
│     "unique_rows_count": 1,                           │
│     "reason": "success",                              │
│     "cost_time": 3.59,                                │
│     "token_usage": {input: 5496, output: 399, ...}    │
│   }                                                   │
└─────────────────────────────────────────────────────┘